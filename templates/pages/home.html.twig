{% extends 'base.html.twig' %}
{% block title 'Agence Lecocq' %}

{% block body %}
    <div class="jumbotron text-center">

        <div class="container">
            <h1>Agence Lecocq</h1>
            <p>Agence Lorem ipsum dolor sit amet, consectetur adipisicing elit. Cupiditate explicabo, facere, fugiat
                iusto, molestias mollitia nobis odio officia quasi quidem quos saepe vero voluptatibus! Laboriosam
                magnam molestiae officiis recusandae rem.</p>
        </div>
    </div>
    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show',{id: property.id , slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                            <div class="text-primary"
                                 style="font-weight: bold;font-size: 2rem">{{ property.formattedPrice }} €
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

    </div>

{% endblock %}